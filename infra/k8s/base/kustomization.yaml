apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: urjakavach
      app.kubernetes.io/part-of: urjakavach

resources:
  - config.yaml
  - api.yaml
  - web.yaml
  - ingress.yaml
